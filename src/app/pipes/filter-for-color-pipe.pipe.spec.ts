import { FilterForColorPipePipe } from './filter-for-color-pipe.pipe';

describe('FilterForColorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterForColorPipePipe();
    expect(pipe).toBeTruthy();
  });
});

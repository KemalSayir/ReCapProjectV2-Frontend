import { FilterForBrandPipePipe } from './filter-for-brand-pipe.pipe';

describe('FilterForBrandPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterForBrandPipePipe();
    expect(pipe).toBeTruthy();
  });
});
